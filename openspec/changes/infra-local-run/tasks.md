## 1. Implementation
- [x] 1.1 Update `pixi.toml` with `redis-server` and `minio` dependencies
- [x] 1.2 Update `pixi.toml` tasks (`build-frontend`, `start-all-linux`, update `setup-linux`)
- [x] 1.3 Create `scripts/start-all-linux.sh`
- [x] 1.4 Windows Support (Completed)
    - [x] Create `scripts/start-all-windows.ps1` (excluding data-processor)
    - [x] Update `pixi.toml` with `start-all-windows` task
    - [x] Update `setup-windows` to exclude `data-processor`
