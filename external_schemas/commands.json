{
  "definitions": {
    "GameCommand": {
      "type": "object",
      "properties": {
        "type": { "const": "game_command" },
        "channel_id": { "type": "integer" },
        "payload": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "enum": ["move up", "move down", "move left", "move right", "attack", "heal"]
            },
            "target_username": { "type": ["string", "null"] },
            "timestamp": { "type": "integer" }
          },
          "required": ["command"]
        }
      },
      "required": ["type", "channel_id", "payload"]
    }
  }
}
