# Google ADK with LiteLLM extensions for provider-style models
google-adk[extensions]>=1.27.0

# LiteLLM for provider-style models (pinned to safe version)
litellm==1.82.6

# FastAPI and dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client for backend calls
httpx>=0.27.0

# JWT validation
python-jose[cryptography]>=3.3.0

# Redis for rate limiting
redis>=5.0.0

# Postgres persistence (claims session data)
psycopg[binary]>=3.1.0
psycopg_pool>=3.1.0
