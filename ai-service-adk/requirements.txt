# Google ADK with LiteLLM extensions for provider-style models
google-adk[extensions]>=1.27.0

# LiteLLM for Claude support via ADK (>=1.75.5 required for provider-style models)
litellm>=1.75.5

# FastAPI and dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client for backend calls
httpx>=0.27.0

# JWT validation
python-jose[cryptography]>=3.3.0

# Redis for rate limiting
redis>=5.0.0
